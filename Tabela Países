create table public.paises (
  id bigint generated always as identity not null,
  nome text not null,
  constraint paises_pkey primary key (id),
  constraint paises_nome_key unique (nome)
) TABLESPACE pg_default;
